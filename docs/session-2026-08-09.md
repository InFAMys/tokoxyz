# Session Summary — Per-Ukuran Harga & UI Polish

Session focus: per-ukuran pricing feature plus a batch of customer/pegawai UI polish.

- opencode: `opencode -s ses_01da2634dffeCIW3TgoBi4BG41`

## Per-Ukuran Prices

- Migration `add_harga_ukuran_to_ukurans_table`: added nullable `harga_ukuran decimal(10,2)` to `ukurans`.
- `Ukuran` model: added `harga_ukuran` to `$fillable`, cast `decimal:2`.
- Route `PUT pegawai/update-harga-ukuran/{id_b}/{id_u}` → `pegawai.uhargau`.
- `UkuranController@updateHargaUkuran`: validate `required|numeric|min:0`, save, per-row flash.
- `k_ukuran` table: new `Harga (Rp)` column with inline per-row PUT form (mirrors stok menu).
- Cart uses ukuran price when set (`KeranjangController`, cart view subtotal + unit price), fallback to `barang.harga`.
- `Ukuran` price now respected in cart via `$item->ukuran?->harga_ukuran ?? $item->barang->harga`.

## Price Display

- Pegawai detail: price header shows ukuran-price range (min–max) over base `harga`; per-ukuran prices added to Ukuran summary-card.
- Customer detail: price range header; per-ukuran price updates instantly on `Pilih Ukuran` select (JS in `script.js`, `data-harga` on options, restore-to-range on empty).
- Customer welcome page: shows cheapest per-ukuran price when present (both Produk Baru + Rekomendasi); ukurans eager-loaded in `home()`.

## Keranjang

- Styled max-stock alert replacing native `<input max>` bubble: `novalidate` form, inline `[data-jumlah-error]` line, JS validate + block submit, absolute-positioned so button doesn't jump.
- `Deskripsi` boxed in summary-box on customer detail.

## Bootstrap Toasts (replaces alert boxes)

- New partial `resources/views/components/toasts.blade.php`: auto-collects all string flash keys + `$errors`, renders top-center toasts.
- Colors: `.toast.success` = pink (`--pink-600`), `.toast.error` = red (`--red-600`).
- Included in customer/owner/pegawai layouts + owner/pegawai standalone login pages.
- ~30 old alert blocks wrapped in Blade comments `{{-- … --}}` (not deleted, for rollback).
- Field-clamped `@error`/invalid-feedback errors kept on inputs.
- Toast moved down (`top:4.5rem`); added countdown progress bar (`<div class="toast-progress">`, width shrinks over `data-bs-delay`); explicit `bootstrap.Toast.getOrCreateInstance(...).hide()` after delay.
- Delay set to 3500ms.

## Harga Input Auto-Format

- 3 inputs (`tambahBarang` harga, `editBarang` harga, `k_ukuran` harga_ukuran) → `type="text" inputmode="numeric"`.
- JS formatter: live id-ID thousand-dot grouping; strips separators on submit for `numeric` validation.
- Fixed decimal-2 bug: DB value `"3000000.00"` → was concatenating to `300.000.000`; `toIntegerDigits()` now drops a real `[.,]+1-2 digit` decimal tail.

## Berat Input Mask

- Add + edit `berat` inputs: live mask allowing digits + single `.`/`,` separator (normalized to comma on display), capped at 3 decimals.
- Server `normalizeBerat` already maps comma→dot before save.

## Pegawai Detail Page Restyle

- Rebuilt `detailBarang` to match customer layout: header = kode + name left / action buttons right; Brand/Kategori/Stok/Berat summary-boxes moved under gallery (left col); right col = name + status badge, price range, Deskripsi/Ukuran/Preorder boxes; dropped redundant `d-flex` wrapper → bare `main-content`.

## Other

- Vite builds throughout (`npm run build`); `view:cache` + `pest` green (2 tests).
- All JS centralized in `resources/js/script.js`.
